---
    - name: install java
      ansible.builtin.import_role:
        name: geerlingguy.java
      become: yes

    - name: install jenkins
      ansible.builtin.import_role:
        name: geerlingguy.jenkins
      become: yes

     